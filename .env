BASE_DIR="$(cd $(dirname "${BASH_SOURCE[1]}") && pwd)"
VARS_DIR="${BASE_DIR}/vars"
TEMPLATES_DIR="${BASE_DIR}/templates"
DNS_PRIMARY=8.8.8.8
DNS_SECONDARY=1.1.1.1
NTPSERVER=0.es.pool.ntp.org
MOTDBANNER="Welcome to privileged nexacore device terminal"